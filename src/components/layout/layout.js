import useStyle from './styles'

const Layout = ()=> {

    const classes = useStyle();

    return (
      <div className = {classes.root}>
        salam
      </div>
    );
  }
  
  export default Layout;
  